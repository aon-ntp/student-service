import { studentProfileCore } from './student-profile-core';

describe('studentProfileCore', () => {
  it('should work', () => {
    expect(studentProfileCore()).toEqual('student-profile-core');
  });
});
