import { studentWishlistApi } from './student-wishlist-api';

describe('studentWishlistApi', () => {
  it('should work', () => {
    expect(studentWishlistApi()).toEqual('student-wishlist-api');
  });
});
