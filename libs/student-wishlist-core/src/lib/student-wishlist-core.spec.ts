import { studentWishlistCore } from './student-wishlist-core';

describe('studentWishlistCore', () => {
  it('should work', () => {
    expect(studentWishlistCore()).toEqual('student-wishlist-core');
  });
});
